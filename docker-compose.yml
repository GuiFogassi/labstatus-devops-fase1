services:
  api:
    build: .
    image: ghcr.io/guifogassi/labstatus-api:latest
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
